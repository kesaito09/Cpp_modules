なんで例外を投げるのか

- 関数の契約（なにを受とって、何を返すか）を明確にするため。

これで起きる問題は、その関数がどこでどんな例外を出すかが関数の宣言を見ただけではわからない。int parse(char *s, int *err)なら引数を見ればエラーがわかるが、例外の場合は実装を全部読む必要がある。

- 不変条件が常に成り立つと言える
    - コンストラクタ内で例外を投げることで、オブジェクトが不変条件を満たすことを保証できる（if で確認しなくてよくなる）
- 例外が発生した部分から、巻き戻しが起きるため、エラー伝播する必要がない
- 

